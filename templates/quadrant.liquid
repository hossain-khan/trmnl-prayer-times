{% comment %}
  ============================================================================
  QUADRANT.LIQUID - Quarter-size Layout Template
  ============================================================================
  Use this template for minimal, compact displays on quarter-size screens.
  
  Best For:
  - Simple, single-value displays
  - Compact widgets in dashboard
  - Minimal information cards
  - Icon + label combinations
  
  Device Sizes: Works across all devices (sm, md, lg)
  Padding: p--1 (minimal - every pixel counts!)
  Responsive: Simplifies further for very small screens
  
  Structure:
  - Single centered content
  - Minimal title bar
  - Absolute space efficiency
  ============================================================================
{% endcomment %}

<div class="layout">
  {% comment %} Check if data is available {% endcomment %}
  {% if has_data %}
  
  {% comment %} 
    Everything centered and minimal
    Uses inline-flex for mixed content display
    See Advanced Layout Patterns section for device-specific optimization
  {% endcomment %}
  <div class="flex flex--col flex--center-x flex--center-y h--full gap--xsmall">
    
    {% comment %} 
      Primary value with tabular numbers for alignment
      data-value-fit auto-scales headline to fit available space
    {% endcomment %}
    <div class="value value--large md:value--xlarge value--tnums" data-value-fit="true" data-value-fit-max-height="140">
      {{ primary_value | default: "42" }}
    </div>
    
    {% comment %} Compact title - uses data-clamp for safety {% endcomment %}
    <div class="title title--small md:title--medium text--center" data-clamp="1">
      {{ metric_name | default: "Quick Stats" }}
    </div>
  </div>

  {% else %}
  
  {% comment %} Display error state when plugin is unconfigured {% endcomment %}
  <div class="flex flex--col flex--center-x flex--center-y gap--xsmall h--full">
    <div class="value value--small md:value--medium">⚙️</div>
    <div class="description text--center">Setup needed</div>
  </div>
  
  {% endif %}
</div>

{% comment %} Minimal title bar for quadrant {% endcomment %}
<div class="title_bar">
  <span class="title title--small">Plugin</span>
</div>
