{% comment %}
  ============================================================================
  HALF_HORIZONTAL.LIQUID - Half-size Horizontal Layout Template
  ============================================================================
  Use this template for side-by-side content on half-width displays.
  
  Best For:
  - Two-column layouts (data + details)
  - Comparative displays
  - Side-by-side information
  
  Device Sizes: Works across all devices (sm, md, lg)
  Padding: p--2 (standard)
  Responsive: Switches to vertical (flex--col) in portrait mode
  
  Structure:
  - Two flex columns with gap
  - Portrait fallback to column layout
  - Compact spacing for limited width
  ============================================================================
{% endcomment %}

<div class="layout">
  {% comment %} Check if data is available {% endcomment %}
  {% if has_data %}
  
  {% comment %} 
    Row layout on landscape, column on portrait
    Left side: main content (flex: 1)
    Right side: details (fixed or constrained width)
  {% endcomment %}
  <div class="flex flex--row gap--medium portrait:flex--col flex--center-y h--full">
    
    {% comment %} Main content area - left side on landscape {% endcomment %}
    <div class="flex flex--center-x flex--center-y" style="flex: 1;">
      <div class="text--center">
        <div class="value value--medium md:value--large lg:value--xlarge value--tnums" data-value-fit="true" data-value-fit-max-height="180">
          42
        </div>
        <div class="title title--small md:title--medium">
          Main Value
        </div>
      </div>
    </div>

    {% comment %} Visual separator for landscape view {% endcomment %}
    <div class="hidden portrait:hidden md:block">
      <div class="divider portrait:hidden"></div>
    </div>

    {% comment %} Secondary content area - right side on landscape {% endcomment %}
    <div class="grid grid--cols-1 gap--xsmall portrait:gap--small portrait:grid--cols-3" style="max-width: 200px; portrait:max-width: 100%;">
      <div class="item text--center">
        <div class="meta"></div>
        <div class="content">
          <div class="label label--small">Detail 1</div>
          <div class="value value--small md:value--medium value--tnums">100</div>
        </div>
      </div>
      <div class="item text--center">
        <div class="meta"></div>
        <div class="content">
          <div class="label label--small">Detail 2</div>
          <div class="value value--small md:value--medium value--tnums">50</div>
        </div>
      </div>
      <div class="item text--center">
        <div class="meta"></div>
        <div class="content">
          <div class="label label--small">Detail 3</div>
          <div class="value value--small md:value--medium value--tnums">75</div>
        </div>
      </div>
    </div>
  </div>

  {% else %}
  
  {% comment %} Display error state when plugin is unconfigured {% endcomment %}
  <div class="flex flex--col flex--center-x flex--center-y gap--small h--full">
    <div class="value value--medium md:value--large">⚙️</div>
    <div class="title title--small md:title--medium text--center">Setup Required</div>
    <div class="description text--center">Configure plugin to begin.</div>
  </div>
  
  {% endif %}
</div>

{% comment %} Title bar at bottom {% endcomment %}
<div class="title_bar">
  <span class="title md:title--medium">Example Plugin</span>
  <span class="instance">Half Horizontal</span>
</div>
