{% comment %}
  ============================================================================
  HALF_VERTICAL.LIQUID - Half-size Vertical Layout Template
  ============================================================================
  Use this template for top-bottom stacked content on half-height displays.
  
  Best For:
  - Stacked layouts (primary + secondary content)
  - Top/bottom split displays
  - Compact vertical layouts
  
  Device Sizes: Works across all devices (sm, md, lg)
  Padding: p--2 (standard)
  
  Structure:
  - Primary content fills available space (flex: 1)
  - Secondary content at bottom
  - Minimal gap to maximize space
  ============================================================================
{% endcomment %}

<div class="layout">
  {% comment %} Check if data is available {% endcomment %}
  {% if has_data %}
  
  <div class="flex flex--col gap--small h--full">
    
    {% comment %}
      Primary content area - fills available space
      Uses richtext pattern for complex, formatted content
      See Advanced Layout Patterns section in copilot-instructions
    {% endcomment %}
    <div class="flex flex--center-x flex--center-y" style="flex: 1;">
      <div class="richtext richtext--center gap--small">
        <div class="content content--center gap--xsmall text--center">
          <div class="value value--xxlarge md:value--xxxlarge" data-clamp="1">
            📊
          </div>
          <div class="title title--medium md:title--large">
            Current Status
          </div>
          <div class="description md:title md:title--small" data-clamp="2">
            {{ status_description | default: "Everything is configured correctly" }}
          </div>
        </div>
      </div>
    </div>

    {% comment %} Modern divider separating top and bottom content {% endcomment %}
    <div class="divider"></div>

    {% comment %}
      Secondary content area - at bottom
      Grid layout provides better metric alignment
      Uses value--tnums for tabular number display
    {% endcomment %}
    <div class="grid grid--cols-3 gap--xsmall text--center">
      <div>
        <div class="label label--small">Last</div>
        <div class="value value--xsmall value--tnums">{{ last_value | default: "—" }}</div>
      </div>
      <div>
        <div class="label label--small">Average</div>
        <div class="value value--xsmall value--tnums">{{ average_value | default: "—" }}</div>
      </div>
      <div>
        <div class="label label--small">Best</div>
        <div class="value value--xsmall value--tnums">{{ best_value | default: "—" }}</div>
      </div>
    </div>
  </div>

  {% else %}
  
  {% comment %} Display error state when plugin is unconfigured {% endcomment %}
  <div class="flex flex--col flex--center-x flex--center-y gap--small h--full">
    <div class="value value--medium md:value--large">⚙️</div>
    <div class="title title--small md:title--medium text--center">Setup Required</div>
    <div class="description text--center">Configure plugin to begin.</div>
  </div>
  
  {% endif %}
</div>

{% comment %} Title bar at bottom {% endcomment %}
<div class="title_bar">
  <span class="title md:title--medium">Example Plugin</span>
  <span class="instance">Half Vertical</span>
</div>
