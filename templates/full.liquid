{% comment %}
  ============================================================================
  FULL.LIQUID - Full-screen Layout Template
  ============================================================================
  Use this template for displaying content across the entire TRMNL screen.
  
  Best For:
  - Detailed information displays
  - Main data presentation
  - Full-screen widgets
  
  Device Sizes: Works across all devices (sm, md, lg)
  Padding: p--2 (standard)
  
  Structure:
  - Main content area with flex: 1 for vertical centering
  - Title bar at bottom for branding
  - Error state fallback for unconfigured plugins
  ============================================================================
{% endcomment %}

<div class="layout">
  {% comment %} Check if data is available {% endcomment %}
  {% if has_data %}
  
  <div class="flex flex--col gap--medium h--full">
    {% comment %} Main content area - centered vertically {% endcomment %}
    <div class="flex flex--center-x flex--center-y" style="flex: 1;">
      <div class="text--center">
        {% comment %} 
          Example: Display your primary data here
          Replace with actual data from your data source
          
          Tip: Use data-value-fit for long headlines that need to scale
          Tip: Use value--tnums for precise numeric alignment
        {% endcomment %}
        <div class="value value--large md:value--xxlarge lg:value--xxxlarge" data-value-fit="true" data-value-fit-max-height="220">
          42
        </div>
        <div class="title title--small md:title--medium lg:title--large">
          Example Value
        </div>
        <div class="description md:label" data-clamp="2">
          This is a template example with better best practices
        </div>
      </div>
    </div>

    {% comment %} Visual separator using modern divider component {% endcomment %}
    <div class="divider"></div>

    {% comment %} Optional secondary content area with improved styling {% endcomment %}
    <div class="grid grid--cols-2 gap--medium md:gap--large">
      <div class="item">
        <div class="meta"></div>
        <div class="content text--center">
          <div class="value value--medium md:value--large">📊</div>
          <div class="label">Stat 1</div>
        </div>
      </div>
      <div class="item">
        <div class="meta"></div>
        <div class="content text--center">
          <div class="value value--medium md:value--large">📈</div>
          <div class="label">Stat 2</div>
        </div>
      </div>
    </div>
  </div>

  {% else %}
  
  {% comment %} Display error state when plugin is unconfigured {% endcomment %}
  <div class="flex flex--col flex--center-x flex--center-y gap--medium h--full">
    <div class="value value--large md:value--xlarge text--center">⚙️</div>
    <div class="title title--medium md:title--large text--center">Setup Required</div>
    <div class="description md:title md:title--small text--center">
      Please configure this plugin with your API key and settings to begin.
    </div>
  </div>
  
  {% endif %}
</div>

{% comment %} Title bar at bottom {% endcomment %}
<div class="title_bar">
  <span class="title md:title--large">Example Plugin</span>
  <span class="instance">Full Layout</span>
</div>
